<Window x:Class="valentine.No"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:valentine"
        mc:Ignorable="d"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="NoResize"
        Background="{x:Null}"
        WindowStartupLocation="CenterScreen"
        Title="No" Height="351" Width="581">
    <Grid>
        <Border Background="LightPink" BorderBrush="{x:Null}" BorderThickness="0" CornerRadius="9">
            <Grid>
                <Button x:Name="exit" Content="X" Foreground="Black" Background="{x:Null}" BorderBrush="{x:Null}" BorderThickness="0" Height="30" Width="30" Margin="542,10,9.6,312" Click="exit_Click"/>
                <Label Content="โดนปฏิเสธ รัก ;( ทำไมต้องพยายามกดมันขนาดนั้นด้วย" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0 0 0 250"/>
                <Image Height="186" Width="250" Margin="165,76,165,30" HorizontalAlignment="Center" VerticalAlignment="Center" Source="gif/1.gif"/>
            </Grid>
        </Border>
    </Grid>
</Window>
